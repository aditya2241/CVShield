services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: trustguard
      POSTGRES_USER: trustguard
      POSTGRES_PASSWORD: trustguard
    ports:
      - "5432:5432"
    volumes:
      - trustguard_pg:/var/lib/postgresql/data
volumes:
  trustguard_pg:
