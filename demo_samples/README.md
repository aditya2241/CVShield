# Safe demo samples

- `clean_demo.txt` should produce a LOW risk result.
- `review_demo.txt` contains harmless text strings that exercise the scanner's defensive content indicators. It is not malware and must not be executed as code.

For the hackathon demo, show the clean sample first, then the review sample and explain that the risk engine uses observable signals rather than claiming definitive malware detection.
